{{#models}}
{{#model}}

{{{vendorExtensions.x-includeDirective}}}

#include <lhmodel/membermeta.h>

#include <array>
#include <unordered_map>

namespace LHModelNS
{
    constexpr std::array< MemberMeta, {{vendorExtensions.x-numVars}} > ModelMembersMeta< {{namespace}}::{{classname}} >::membersMeta;

    const std::unordered_map< std::string, size_t > ModelMembersMeta< {{namespace}}::{{classname}} >::memberIndex{ {
    {{#vars}}
    { "{{name}}", {{vendorExtensions.x-varIndex}} }{{#hasMore}},{{/hasMore}}{{^hasMore}} } };{{/hasMore}}
    {{/vars}}

    constexpr ModelMeta ModelModelMeta< {{namespace}}::{{classname}} >::modelMeta;
}

// Swagger Codegen OpenAPI v3 LHModelServerCodegen v{{lhmodelServerCodegenVersion}}

{{/model}}
{{/models}}
